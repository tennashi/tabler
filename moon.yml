$schema: ".moon/cache/schemas/project.json"

project:
  description: "Repository root"

tasks:
  format-check:
    inputs:
      - ".agent/**"
      - ".claude/**"
      - "*"
      - ".*"
      - "!.git/**"
      - "!.moon/**"
    options:
      mergeInputs: "append"

  lint:
    inputs:
      - ".agent/**/*.md"
      - ".claude/**/*.md"
      - "*.md"
    options:
      mergeInputs: "append"
